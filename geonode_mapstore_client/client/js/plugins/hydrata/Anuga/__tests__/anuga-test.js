
import expect from 'expect';

describe('Anuga Plugin', () => {
    beforeEach(done => {
        setTimeout(done);
    });
    afterEach(done => {
        setTimeout(done);
    });
    it('should render Anuga menus', () => {
        const a = 1;
        const b = 2;
        expect(a + b).toBe(3);
    });
});
